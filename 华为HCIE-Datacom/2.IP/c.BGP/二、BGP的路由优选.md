·```
1.优选Preferred-Value属性 值最大的路由。￼         华为的私有属性 （对于值默认为0，越大越优先的） 本地有效的属性，只能作用邻居接收路由的入方向  
2.优选Local_Preference 属性值最大的路由。
```

```
↑  取值越大越优
```

```
3.本地始发的BGP路由优于从其他对等体学习到的路由，本地始发的路由优先级：优选手动聚合\>自动聚合\>network\>import\>从对等体学到的。  
 （下一跳 127.0.0.1  \>  0.0.0.0  \> 邻居的更新源地址）  
4.优选AS_Path属性值最短的路由。  
5.优选Origin属性最优的路由。Origin属性值按优先级从高到低的排列是：IGP、EGP及Incomplete。  
6.优选MED属性值最小的路由。  
7.优选从EBGP对等体学来的路由（EBGP路由优先级高于IBGP路由）。                                           ↓ 取值越小越优
```

![Exported image](Exported%20image%2020251206164618-0.png)

```
8.优选到Next_Hop的IGP度量值最小的路由。      
```

![Exported image](Exported%20image%2020251206164619-1.png)

```
9.优选Cluster_List最短的路由。                                                             当前8条属性全部相同时可以形成路由负载分担  
10.优选Router ID（Orginator_ID）最小的设备通告的路由。  
11.优选具有最小IP地址的对等体通告的路由。  
优选具有最小IP地址的对等体通告的路由  
优选邻居IP地址小的路由
```
 
```
当到达同一个目的网段存在多条路由时，BGP通过如下的次序进行路由优选：  
      丢弃下一跳不可达的路由。
```

```
PLLAOMEN（漂亮老男人）
```

```
（ 当前8条属性全部相同时可以形成路由负载分担）￼对于路由选路规则 前8条都相等的情况下，则路由可以形成负载分担的路由  
*默认BGP路由只会将最优的一条加入到IP路由表中
```
 
```
在BGP中执行多条路由进行负载分担  
[AR2-bgp]maximum load-balancing ibgp 2 
```
 
```
该方式只会对BGP路由加入IP路由表的影响  
不会影响BGP路由的传递（BGP只会传递最优的路由条目）（BGP一定能选出最优的路由条目）
```