```
区域间的防环规则：  
1.拓扑防环  
   要求非骨干区域必须和骨干区域连接  
   要求只能存在一个骨干区域  
2.ABR防环  
   ABR接收非骨干区域的3类LSA，不会计算路由条目  
   ABR接收骨干区域的3类LSA，会向连接的其他所有非骨干区域进行泛洪  
3.LSA的优选
```

![Exported image](Exported%20image%2020251206152402-0.png)  

```
如图1，当5.5.5.5向Area0发送自己的LSA时，会同时向两个ABR发送，两个ABR也会把1，2类LSA转换为3类LSA，同时骨干区域的AR会向两个ABR泛红3类LSA，同时也携带1，2 类LSA，但是到了ABR，只会转发1，2类LSA，不会将3类LSA泛红到Area1  
***1类、2类LSA 优于 3类LSA  
 当1类、2类LSA携带的路由信息 和 3类LSA携带的路由信息相同，则优选1类、2类的
```
   

```
如图：6.6.6.6去访问AR3，走的是Area0
```

![Exported image](Exported%20image%2020251206152404-1.png)  

```
***骨干区域的3类LSA优于非骨干区域的3类LSA  
（和链路cost的大小无关）
```
   

```
区域内的防环规则：  
   SPF计算最优路径不存在环路  
如图3，如果Area1想要访问Area2，是怎么的一个访问路径？
```

![Exported image](Exported%20image%2020251206152405-2.png)  

```
1.首先了解ABR的作用，将1，2类LSA信息转换为3类LSA信息，可得，来自Area0和Area1的LSA信息都会同时转换为3类LSA信息泛洪到Area2，所以说Area2想访问Area1，直接访问，不会走Area0 去绕行 
```